import type { CheckoutItem } from "../../../domain/checkout/checkout.api";

const CheckoutItemRow = ({ checkoutItem }: { checkoutItem: CheckoutItem }) => {
  return <div>주문 상품 #</div>;
};

export default CheckoutItemRow;
